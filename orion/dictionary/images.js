orion.dictionary.addDefinition('muebles', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría muebles',
      optional: true
  })
);

orion.dictionary.addDefinition('pisos-revestimientos', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría pisos & revestimientos',
      optional: true
  })
);

orion.dictionary.addDefinition('decoracion', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría decoración',
      optional: true
  })
);

orion.dictionary.addDefinition('iluminacion', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría iluminación',
      optional: true
  })
);

orion.dictionary.addDefinition('exterior', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría exterior',
      optional: true
  })
);

orion.dictionary.addDefinition('banos', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría baños',
      optional: true
  })
);

orion.dictionary.addDefinition('cocina', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría cocina',
      optional: true
  })
);

orion.dictionary.addDefinition('infantil', 'images',
  orion.attribute('image', {
      label: 'Imagen categoría infantil',
      optional: true
  })
);

orion.dictionary.addDefinition('decoradores', 'images',
  orion.attribute('image', {
      label: 'Imagen decoradores',
      optional: true
  })
);
